RewriteCond %{SERVER_PORT} 80
RewriteCond %{HTTP_HOST} ^(www\.)?cancode\.us
RewriteRule ^(.*)$ https://www.cancode.us/$1 [R,L]

// Do not copy this line, place these right under RewritEngine On and above everything else
